export const AUDIO_DEFAULT_VALUE = {
  autoplay: false,
  enabledKeyboardControl: false,
  loop: false,
  maxProgressTime: 100,
  maxProgressVolume: 1,
  src: '',
  timeShift: 10,
} as const;

export const DEFAULT_UNMUTE_VOLUME = 0.01;
